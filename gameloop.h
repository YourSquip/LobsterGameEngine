#ifndef GAMELOOP_H
#define GAMELOOP_H

#include <QTimer>
/*
class GameLoop
{
public:

    static GameLoop& get_instance()
    {
        static GameLoop instance = GameLoop();
        return instance;
    }

    void run()
    {
        while(m_loop_is_running)
        {
            QTime currentTime= QTime::currentTime();

        }
    }


private:
    QTimer* m_timer;

    bool m_loop_is_running;

    GameLoop(GameLoop const&) = delete;
    GameLoop& operator= (GameLoop const&) = delete;
};*/

#endif // GAMELOOP_H
